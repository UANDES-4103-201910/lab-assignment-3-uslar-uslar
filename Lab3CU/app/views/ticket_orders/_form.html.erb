<%= form_for(@ticket_order) do |f| %>
  <% if @ticket_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_order.errors.count, "error") %> prohibited this ticket_order from being saved:</h2>

      <ul>
      <% @ticket_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount_payed %><br>
    <%= f.number_field :amount_payed %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
